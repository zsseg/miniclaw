# `tests` 测试说明

本目录包含 Clawmini 的基础单元测试，覆盖：

- 安全能力（注入过滤、路径越界拦截）
- 写作工具（创建、全文预览、段落预览、提交、撤销、批量任务进度）
- Agent 主流程（Mock 模型 + ReAct + 工具执行）
- QQ 工具扩展（网关切换与配置更新）

## 运行目标

确保核心功能在本地可稳定运行，且不依赖在线模型。
